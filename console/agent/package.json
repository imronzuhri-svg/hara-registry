{
  "name": "hara-registry-console-agent",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Read-only backups-status agent (dependency-free; plain node:http). Reports this host's hara-*-snapshot timer status.",
  "scripts": {
    "start": "node src/index.mjs",
    "typecheck": "node --check src/index.mjs"
  }
}
